# Process Report: SAF data layer — SafPath, NamingMacro, MacroExpander (copy-inbox)

## What Was Asked
- **Drift Check**: Confirm HEAD is `5faa51c` on `inscope-labs/copy-inbox`.
- **1. SafPath Room entity**: Create `SafPath` entity (`saf_paths` table) with `id`, `label`, `treeUri`, `lastUsedAt`, `seqCounter`.
- **2. NamingMacro Room entity**: Create `NamingMacro` entity (`naming_macros` table) with `id`, `label`, `template`, `createdAt`.
- **3. SafPathDao**: Create `SafPathDao` with `insert`, `update`, `delete`, `observeAll`, `lastUsed`, `recordUse`.
- **4. NamingMacroDao**: Create `NamingMacroDao` with `insert`, `update`, `delete`, `observeAll`.
- **5. Database Migration v3 -> v4**: Update `ClipboardDatabase` to version 4, include `SafPath` and `NamingMacro` entities, add `MIGRATION_3_4`, and expose `safPathDao()` and `namingMacroDao()`.
- **6. MacroExpander**: Implement token replacement (`{date}`, `{time}`, `{type}`, `{index}`, `{seq}`, `{hash}`) and filename sanitization.
- **7. SafExporter**: Implement SAF file creation using `DocumentsContract` and `openOutputStream`.
- **8. SafPathRepository**: Wrap `SafPathDao` and `NamingMacroDao` for flow observation and mutations.
- **9. ClipInBoxApplication Wiring**: Instantiate and expose `safPathRepository`.

## What Was Changed
- **`app/src/main/java/com/inscopelabs/abx/clipinbox/data/local/SafPath.kt`**: Created `SafPath` entity.
- **`app/src/main/java/com/inscopelabs/abx/clipinbox/data/local/NamingMacro.kt`**: Created `NamingMacro` entity.
- **`app/src/main/java/com/inscopelabs/abx/clipinbox/data/local/SafPathDao.kt`**: Created `SafPathDao` interface.
- **`app/src/main/java/com/inscopelabs/abx/clipinbox/data/local/NamingMacroDao.kt`**: Created `NamingMacroDao` interface.
- **`app/src/main/java/com/inscopelabs/abx/clipinbox/data/local/ClipboardDatabase.kt`**: Added entities, bumped version to 4, added DAOs and `MIGRATION_3_4`.
- **`app/src/main/java/com/inscopelabs/abx/clipinbox/export/saf/MacroExpander.kt`**: Implemented macro expansion logic and filename sanitization.
- **`app/src/main/java/com/inscopelabs/abx/clipinbox/export/saf/SafExporter.kt`**: Implemented SAF file exporter using `DocumentsContract`.
- **`app/src/main/java/com/inscopelabs/abx/clipinbox/export/saf/SafPathRepository.kt`**: Created `SafPathRepository`.
- **`app/src/main/java/com/inscopelabs/abx/clipinbox/ClipInBoxApplication.kt`**: Wired `safPathRepository` property and initialization.

## Migration SQL (Verbatim)
```sql
CREATE TABLE IF NOT EXISTS `saf_paths` (
  `id` INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
  `label` TEXT NOT NULL,
  `treeUri` TEXT NOT NULL,
  `lastUsedAt` INTEGER NOT NULL DEFAULT 0,
  `seqCounter` INTEGER NOT NULL DEFAULT 0
)

CREATE TABLE IF NOT EXISTS `naming_macros` (
  `id` INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
  `label` TEXT NOT NULL,
  `template` TEXT NOT NULL,
  `createdAt` INTEGER NOT NULL
)
```

## Mandatory Confirmations
- **HEAD Confirmation**: Git commit HEAD verified at `5faa51c5faa51cc4a05b40bbae29bfb49af83fa1`.
- **SafExporter Engine**: Confirmed `SafExporter` uses `DocumentsContract.buildDocumentUriUsingTree()`, `DocumentsContract.createDocument()`, and `contentResolver.openOutputStream()` (no java.io.File API used).
- **MacroExpander Sanitization**: Confirmed `sanitizeFilename()` replaces regex `[/\\:*?"<>|]` with `_` and trims whitespace.
- **Build Verification**: `assembleDebug` compiled successfully with `compile_applet`.

## Commands Executed & Results
- `git rev-parse --short HEAD; git rev-parse HEAD`: Confirmed commit hash `5faa51c`.
- `compile_applet`: Build succeeded.

## Assumptions
- None.

## Errors / Failures / Partial Failures
- None.
