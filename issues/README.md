# Issue Tracking Directory

This directory holds discrete issue tracking files per **AGENTS.md Section 1.1**.

- **`issues/pending/`**: Holds open/unresolved issue files.
- **`issues/resolved/`**: Holds closed issue files moved here via `git mv` (never deleted) to maintain an audit trail.
