# Resolved Issues Directory

This directory holds resolved issue files moved from `issues/pending/` via `git mv` per **AGENTS.md Section 1.1**.

Issue files are retained here permanently as an immutable audit trail and are never deleted.
